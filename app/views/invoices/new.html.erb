<%= render 'shared/errors', object: @invoice %>

<%= form_for [@user, @invoice] do |form| %>
    <%= render partial: 'fields', locals: { f: form } %>

    <%= form.submit 'Create new invoice', :class => "btn btn-success" %>
<% end %>