<h1>New payment</h1>

<%= render 'shared/errors', object: @payment %>

<%= form_for [@invoice, @payment] do |form| %>
    <%= render partial: 'fields', locals: { f: form } %>

    <%= form.submit 'Create new payment', :class => "btn btn-success" %>
<% end %>